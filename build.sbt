ThisBuild / scalaVersion := "3.0.0"
ThisBuild / organization := "dev.ligature"
ThisBuild / name := "raccoon"
ThisBuild / version := "0.1.0-SNAPSHOT"

lazy val root = project
  .in(file("."))
  .settings(
  )
